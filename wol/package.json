{
  "name": "workoutlink",
  "version": "0.1.0",
  "private": true,
  "description": "WorkOutLink single-publisher PoC",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute workoutlink-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute workoutlink-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
